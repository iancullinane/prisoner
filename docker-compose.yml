services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: prisoner
      POSTGRES_PASSWORD: prisoner
      POSTGRES_DB: prisoner
    ports:
      - "5432:5432"
    volumes:
      - ./db/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
